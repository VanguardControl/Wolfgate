# Fading-out view of a body close to death (WolfmedDyingEffectsSystem).
- type: shader
  id: WFWolfmedDying
  kind: source
  path: "/Textures/_WF/Wolfmed/Shaders/dying.swsl"

# HUD: the cyan edge tint, torn slice and standby drain behind the synthetic readout.
- type: shader
  id: WFWolfmedSynthetic
  kind: source
  path: "/Textures/_WF/Wolfmed/Shaders/synthetic.swsl"
