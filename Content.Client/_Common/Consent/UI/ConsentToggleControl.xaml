<!--
SPDX-FileCopyrightText: Copyright (c) 2024-2025 Space Wizards Federation
SPDX-License-Identifier: MIT
-->

<Control
    xmlns="https://spacestation14.io" HorizontalExpand="True"
    xmlns:gfx="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client">
    <PanelContainer HorizontalExpand="True" MinWidth="200" Margin="5">
        <PanelContainer.PanelOverride>
            <gfx:StyleBoxFlat BackgroundColor="#1B1B1E" />
        </PanelContainer.PanelOverride>
        <BoxContainer Orientation="Vertical" HorizontalExpand="True" Margin="5">
            <!-- WOLFGATE: the name takes the spare width and wraps, and the buttons keep their size, so a long
                 name in a narrow pane no longer squashes them. -->
            <BoxContainer Orientation="Horizontal" Margin="5">
                <RichTextLabel Name="ConsentToggleNameLabel" HorizontalExpand="True" />
                <Button
                    Name="ConsentToggleOffButton"
                    MinWidth="44"
                    ToggleMode="True"
                    Access="Public"
                    Text="Off"
                    StyleClasses="OpenRight" />
                <Button
                    Name="ConsentToggleOnButton"
                    MinWidth="44"
                    ToggleMode="True"
                    Access="Public"
                    Text="On"
                    StyleClasses="OpenLeft" />
            </BoxContainer>
            <RichTextLabel Name="ConsentToggleDescriptionLabel" />
        </BoxContainer>
    </PanelContainer>
</Control>
